{
  "name": "classbridge-ai",
  "version": "0.1.0",
  "private": true,
  "description": "ClassBridge AI - A Gemma-powered offline mobile classroom for low-connectivity schools.",
  "workspaces": [
    "hub",
    "mobile"
  ],
  "scripts": {
    "install:all": "npm install && npm install --prefix hub && npm install --prefix mobile",
    "dev:hub": "npm run dev --prefix hub",
    "dev:mobile": "npm run start --prefix mobile",
    "start:hub": "npm run start --prefix hub",
    "build:hub": "npm run build --prefix hub",
    "seed": "npm run seed --prefix hub",
    "reset:hub": "npm run reset --prefix hub",
    "test": "npm run test --prefix hub",
    "test:hub": "npm run test --prefix hub",
    "typecheck": "npm run typecheck --prefix hub && npm run typecheck --prefix mobile"
  },
  "license": "MIT"
}
